import { emailsSearch } from "./emailsAPI";
import { TermType } from "@/types/interface";

// describe('Example test suite', () => {
//     test('must work', () => {
//       expect(emailsSearch(Term={term:"allen"}: <TermType>,0,25)).toBe(42)
//     })
//   })